# doc 3\-数据库设计说明书

> 项目名称：AgenticCommerce（自主智能体电商）
> 版本：v1\.2
> 编制日期：2026\-09\-23
> 编制方：飞流智能科技
> 客户：环球出海科技有限公司
> 文档性质：数据库设计文档，定义 AgenticCommerce 平台的数据库规范、表结构、索引策略、数据流转机制与初始化脚本。
> 技术栈：PostgreSQL 16 \+ pgvector \+ Redis
> 
> 

---

# 数据库设计规范

## 1\.1 命名规范

## 1\.2 主键规范

- 主键类型：`varchar(32)`

- 默认值：`replace(gen_random_uuid()::text, '-', '')`

- 关联字段：同样使用 `varchar(32)`

## 1\.3 审计字段规范

每张业务表必须包含以下 7 个审计字段：

## 1\.4 外键策略

不创建数据库级外键约束，通过 `xxx_id` 字段逻辑关联，由应用层保证引用完整性，通过索引加速关联查询。

## 1\.5 索引策略

## 1\.6 多租户隔离

- 所有业务表包含 `tenant_id varchar(32) NOT NULL`；

- 所有查询强制附加 `tenant_id` 过滤；

- 复合索引以 `tenant_id` 为首列；

- RAG 向量检索强制附加 `tenant_id` 过滤；

- 可选启用行级安全策略（RLS）作为额外防线。

## 1\.7 向量存储规范

- 向量维度：1024（千问 text\-embedding\-v4 / BGE\-large\-zh\-v1\.5）；

- 距离函数：余弦距离（`vector_cosine_ops`）；

- 索引类型：HNSW；

- HNSW 参数：`m=16, ef_construction=64`；

- 元数据：`metadata jsonb`。

---

# ER 图

## 2\.1 核心实体中文对照

## 2\.2 完整 ER 图

```Plain Text
┌──────────────────────────────────────────────────────────────────────────┐
│                          租户层（多租户隔离）                              │
│                                                                          │
│   ┌─────────────────────┐                                                │
│   │      租户表          │                                                │
│   │    ac_tenant        │                                                │
│   │  ─────────────      │                                                │
│   │  id (PK)            │                                                │
│   │  name 租户名称       │                                                │
│   │  status 状态         │                                                │
│   │  plan 套餐           │                                                │
│   └──────────┬──────────┘                                                │
│              │ 1:N                                                       │
└──────────────┼───────────────────────────────────────────────────────────┘
               │
               │ tenant_id 挂载到所有业务表
               │
   ┌───────────┼───────────────────────────────────────────────────────┐
   │           │                                                       │
   ▼           ▼                                                       ▼
┌──────────┐ ┌──────────────┐                              ┌──────────────────┐
│  用户表   │ │  模型厂商表   │                              │   知识库文档表    │
│ ac_user  │ │ ac_provider  │                              │ac_knowledge_base │
│──────────│ │──────────────│                              │      _doc        │
│id (PK)   │ │id (PK)       │                              │──────────────────│
│tenant_id │ │tenant_id     │                              │id (PK)           │
│name 姓名 │ │type 类型      │                              │tenant_id         │
│email 邮箱│ │  llm/image/  │                              │category 分类      │
│password  │ │  video       │                              │doc_type 文档类型  │
│role 角色 │ │name 厂商名    │                              │name 文档名称      │
│status    │ │api_key_enc   │                              │vector_status     │
│          │ │is_default    │                              │  向量化状态        │
└──────────┘ └──────────────┘                              │chunk_count       │
                                                           │  分块数量         │
                                                           └────────┬─────────┘
                                                                    │ 1:N
                                                                    ▼
                                                           ┌──────────────────┐
                                                           │   知识库分块表    │
                                                           │ac_knowledge_chunk│
                                                           │──────────────────│
                                                           │id (PK)           │
                                                           │tenant_id         │
                                                           │doc_id (FK逻辑)    │
                                                           │chunk_index 序号   │
                                                           │content 文本内容   │
                                                           │embedding 向量     │
                                                           │  vector(1024)    │
                                                           │doc_type 文档类型  │
                                                           │metadata 元数据    │
                                                           └──────────────────┘

┌──────────────────────────────────────────────────────────────────────────┐
│                          业务层（任务与生成）                              │
│                                                                          │
│   ┌─────────────────┐                                                    │
│   │   商品SKU表      │                                                    │
│   │    ac_sku       │                                                    │
│   │─────────────────│                                                    │
│   │ id (PK)         │                                                    │
│   │ tenant_id       │                                                    │
│   │ sku_code 编码    │                                                    │
│   │ name 商品名称    │                                                    │
│   │ platform 平台    │                                                    │
│   │ category 类目    │                                                    │
│   │ status 状态      │                                                    │
│   └────────┬────────┘                                                    │
│            │ 1:N                                                         │
│            │                                                             │
│   ┌────────▼────────┐         ┌─────────────────┐                        │
│   │    批次表        │ 1:N     │   生成任务表     │                        │
│   │   ac_batch      ├────────►│    ac_task      │                        │
│   │─────────────────│         │─────────────────│                        │
│   │ id (PK)         │         │ id (PK)         │                        │
│   │ tenant_id       │         │ tenant_id       │                        │
│   │ name 批次名称    │         │ batch_id (FK)   │                        │
│   │ sku_count 数量   │         │ sku_id (FK)     │                        │
│   │ config 配置      │         │ config 任务配置  │                        │
│   │ status 状态      │         │ status 状态      │                        │
│   └─────────────────┘         │ progress 进度    │                        │
│                               │ current_node    │                        │
│                               │  当前节点        │                        │
│                               │ retry_count     │                        │
│                               │  重试次数        │                        │
│                               │ trace_id 追踪ID  │                        │
│                               │ total_token_use │                        │
│                               │ total_cost 费用  │                        │
│                               └────────┬────────┘                        │
│                                        │ 1:N                             │
│            ┌───────────────────────────┼───────────────────────────┐     │
│            │                           │                           │     │
│            ▼                           ▼                           ▼     │
│   ┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐│
│   │   文案内容表     │       │   素材资产表     │       │  合规报告表      ││
│   │    ac_copy      │       │    ac_asset     │       │ac_compliance_   ││
│   │─────────────────│       │─────────────────│       │    report       ││
│   │ id (PK)         │       │ id (PK)         │       │─────────────────││
│   │ tenant_id       │       │ tenant_id       │       │ id (PK)         ││
│   │ task_id (FK)    │       │ task_id (FK)    │       │ tenant_id       ││
│   │ sku_id (FK)     │       │ sku_id (FK)     │       │ task_id (FK)    ││
│   │ language 语言    │       │ type 类型        │       │ sku_id (FK)     ││
│   │ title 标题       │       │  image/video/   │       │ score 总分       ││
│   │ bullets 五点描述 │       │  storyboard     │       │ dimensions      ││
│   │ description     │       │ sub_type 子类型  │       │  各维度得分      ││
│   │  长描述          │       │ url 存储路径     │       │ violations      ││
│   │ keywords 关键词  │       │ meta 元数据      │       │  违规项列表      ││
│   │ status 状态      │       │ status 状态      │       │ passed 是否通过  ││
│   └─────────────────┘       │ is_mock 是否Mock │       │ suggestions     ││
│                              └─────────────────┘       │  建议列表        ││
│                                                        └─────────────────┘│
│                                                                          │
│   ┌──────────────────────────────────────────────────────────────────┐  │
│   │                      节点执行记录表                                │  │
│   │                    ac_task_node_run                              │  │
│   │  ────────────────────────────────────────────────────────────    │  │
│   │  id (PK)                                                         │  │
│   │  tenant_id                                                       │  │
│   │  task_id (FK) → ac_task.id                                       │  │
│   │  node_name 节点名:                                                │  │
│   │    orchestrator 编排调度                                          │  │
│   │    requirement_analyzer 需求分析                                  │  │
│   │    creative_planner 创意策划                                      │  │
│   │    visual_designer 视觉设计                                       │  │
│   │    image_generator 图片生成                                       │  │
│   │    video_generator 视频生成                                       │  │
│   │    quality_reviewer 质量审核                                      │  │
│   │  status 状态: running/success/failed                              │  │
│   │  input 输入快照                                                   │  │
│   │  output 输出快照                                                  │  │
│   │  prompt_system System提示词                                       │  │
│   │  prompt_human Human提示词                                         │  │
│   │  elapsed_ms 耗时                                                  │  │
│   │  token_usage Token消耗                                            │  │
│   │  cost 费用                                                        │  │
│   │  upstream 上游节点                                                │  │
│   │  downstream 下游节点                                              │  │
│   └──────────────────────────────────────────────────────────────────┘  │
│                                                                          │
│   ┌─────────────────┐       ┌─────────────────┐                          │
│   │  操作日志表      │       │  刊登记录表      │                          │
│   │ ac_operation_log│       │   ac_listing    │                          │
│   │─────────────────│       │  （二期）        │                          │
│   │ id (PK)         │       │─────────────────│                          │
│   │ tenant_id       │       │ id (PK)         │                          │
│   │ user_id 操作人   │       │ tenant_id       │                          │
│   │ action 动作      │       │ sku_id (FK)     │                          │
│   │ target_type     │       │ platform 平台    │                          │
│   │ target_id       │       │ external_id     │                          │
│   │ detail 详情      │       │  平台商品ID      │                          │
│   │ ip_address IP   │       │ status 状态      │                          │
│   │ user_agent UA   │       │ error 错误信息   │                          │
│   └─────────────────┘       └─────────────────┘                          │
└──────────────────────────────────────────────────────────────────────────┘
```

## 2\.3 关系说明

---

# 表结构详细设计

## 3\.1 ac\_tenant — 租户表

索引：

- `idx_ac_tenant_status` ON \(status\)

- `idx_ac_tenant_plan` ON \(plan\)

## 3\.2 ac\_user — 用户表

索引：

- `uq_ac_user_tenant_email` UNIQUE ON \(tenant\_id, email\) WHERE is\_deleted = 0

- `idx_ac_user_tenant_role` ON \(tenant\_id, role\)

- `idx_ac_user_status` ON \(status\)

## 3\.3 ac\_provider — 模型厂商表

索引：

- `idx_ac_provider_tenant_type` ON \(tenant\_id, type\)

- `idx_ac_provider_tenant_default` ON \(tenant\_id, type, is\_default\) WHERE is\_deleted = 0

- `chk_ac_provider_type` CHECK \(type IN \('llm','image','video'\)\)

## 3\.4 ac\_knowledge\_base\_doc — 知识库文档表

索引：

- `idx_ac_kb_doc_tenant_category` ON \(tenant\_id, category\)

- `idx_ac_kb_doc_tenant_status` ON \(tenant\_id, vector\_status\)

- `idx_ac_kb_doc_tenant_type` ON \(tenant\_id, doc\_type\)

## 3\.5 ac\_knowledge\_chunk — 知识库分块表（向量）

索引：

- `idx_ac_kb_chunk_tenant_doc` ON \(tenant\_id, doc\_id\)

- `idx_ac_kb_chunk_tenant_type` ON \(tenant\_id, doc\_type\)

- `idx_ac_kb_chunk_metadata` GIN ON \(metadata\)

- `idx_ac_kb_chunk_embedding_hnsw` HNSW ON \(embedding vector\_cosine\_ops\) WITH \(m=16, ef\_construction=64\)

## 3\.6 ac\_sku — 商品SKU表

索引：

- `uq_ac_sku_tenant_code` UNIQUE ON \(tenant\_id, sku\_code\) WHERE is\_deleted = 0

- `idx_ac_sku_tenant_platform` ON \(tenant\_id, platform\)

- `idx_ac_sku_tenant_status` ON \(tenant\_id, status\)

## 3\.7 ac\_batch — 批次表

索引：

- `idx_ac_batch_tenant_status` ON \(tenant\_id, status\)

- `idx_ac_batch_tenant_time` ON \(tenant\_id, create\_time DESC\)

## 3\.8 ac\_task — 生成任务表（核心表）

索引：

- `idx_ac_task_tenant_batch` ON \(tenant\_id, batch\_id\)

- `idx_ac_task_tenant_sku` ON \(tenant\_id, sku\_id\)

- `idx_ac_task_tenant_status` ON \(tenant\_id, status\)

- `idx_ac_task_tenant_time` ON \(tenant\_id, create\_time DESC\)

- `idx_ac_task_trace_id` ON \(trace\_id\) WHERE trace\_id IS NOT NULL

## 3\.9 ac\_task\_node\_run — 节点执行记录表（核心表）

索引：

- `idx_ac_tnr_tenant_task` ON \(tenant\_id, task\_id\)

- `idx_ac_tnr_tenant_node` ON \(tenant\_id, node\_name\)

- `idx_ac_tnr_tenant_status` ON \(tenant\_id, status\)

- `idx_ac_tnr_create_time` ON \(create\_time DESC\)

## 3\.10 ac\_copy — 文案内容表

索引：

- `idx_ac_copy_tenant_task` ON \(tenant\_id, task\_id\)

- `idx_ac_copy_tenant_sku` ON \(tenant\_id, sku\_id\)

- `idx_ac_copy_tenant_status` ON \(tenant\_id, status\)

## 3\.11 ac\_asset — 素材资产表

索引：

- `idx_ac_asset_tenant_task` ON \(tenant\_id, task\_id\)

- `idx_ac_asset_tenant_sku` ON \(tenant\_id, sku\_id\)

- `idx_ac_asset_tenant_type_status` ON \(tenant\_id, type, status\)

## 3\.12 ac\_compliance\_report — 合规报告表

索引：

- `idx_ac_cr_tenant_task` ON \(tenant\_id, task\_id\)

- `idx_ac_cr_tenant_sku` ON \(tenant\_id, sku\_id\)

- `idx_ac_cr_tenant_passed` ON \(tenant\_id, passed\)

## 3\.13 ac\_operation\_log — 操作日志表

索引：

- `idx_ac_olog_tenant_user` ON \(tenant\_id, user\_id\)

- `idx_ac_olog_tenant_action` ON \(tenant\_id, action\)

- `idx_ac_olog_create_time` ON \(create\_time DESC\)

## 3\.14 ac\_listing — 刊登记录表（二期）

索引：

- `idx_ac_listing_tenant_sku` ON \(tenant\_id, sku\_id\)

- `idx_ac_listing_tenant_platform` ON \(tenant\_id, platform\)

- `idx_ac_listing_tenant_status` ON \(tenant\_id, status\)

# 完整初始化脚本（init\.sql）

```SQL
-- ============================================================
-- AgenticCommerce 数据库初始化脚本
-- 版本: v1.0
-- 数据库: PostgreSQL 16 + pgvector
-- ============================================================

-- 0. 扩展
CREATE EXTENSION IF NOT EXISTS vector;

-- ============================================================
-- 1. 表结构
-- ============================================================

-- 1.1 ac_tenant 租户表
CREATE TABLE IF NOT EXISTS ac_tenant (
    id                  varchar(32)  NOT NULL DEFAULT replace(gen_random_uuid()::text,'-',''),
    name                varchar(200) NOT NULL,
    status              varchar(20)  NOT NULL DEFAULT 'active',
    plan                varchar(50)  DEFAULT 'standard',
    contact_email       varchar(255),
    contact_phone       varchar(50),
    max_skus            integer      DEFAULT 10000,
    max_tokens_daily    bigint       DEFAULT 1000000,
    metadata            jsonb        DEFAULT '{}',
    create_user_id      varchar(32),
    create_user_name    varchar(100),
    update_user_id      varchar(32),
    update_user_name    varchar(100),
    create_time         timestamptz  NOT NULL DEFAULT now(),
    update_time         timestamptz  NOT NULL DEFAULT now(),
    is_deleted          smallint     NOT NULL DEFAULT 0,
    CONSTRAINT pk_ac_tenant PRIMARY KEY (id),
    CONSTRAINT chk_ac_tenant_status CHECK (status IN ('active','suspended','disabled'))
);
CREATE INDEX IF NOT EXISTS idx_ac_tenant_status ON ac_tenant (status);
CREATE INDEX IF NOT EXISTS idx_ac_tenant_plan ON ac_tenant (plan);
COMMENT ON TABLE  ac_tenant IS '租户表：多租户隔离的基本单元';
COMMENT ON COLUMN ac_tenant.id IS '租户ID（32位UUID）';
COMMENT ON COLUMN ac_tenant.name IS '租户名称';
COMMENT ON COLUMN ac_tenant.status IS '状态：active/suspended/disabled';
COMMENT ON COLUMN ac_tenant.plan IS '套餐：standard/professional/enterprise';
COMMENT ON COLUMN ac_tenant.contact_email IS '联系人邮箱';
COMMENT ON COLUMN ac_tenant.contact_phone IS '联系电话';
COMMENT ON COLUMN ac_tenant.max_skus IS 'SKU配额上限';
COMMENT ON COLUMN ac_tenant.max_tokens_daily IS '日Token配额';
COMMENT ON COLUMN ac_tenant.metadata IS '扩展元数据(JSONB)';
COMMENT ON COLUMN ac_tenant.create_user_id IS '创建人ID';
COMMENT ON COLUMN ac_tenant.create_user_name IS '创建人姓名';
COMMENT ON COLUMN ac_tenant.update_user_id IS '更新人ID';
COMMENT ON COLUMN ac_tenant.update_user_name IS '更新人姓名';
COMMENT ON COLUMN ac_tenant.create_time IS '创建时间';
COMMENT ON COLUMN ac_tenant.update_time IS '更新时间';
COMMENT ON COLUMN ac_tenant.is_deleted IS '逻辑删除：0=正常 1=已删除';

-- 1.2 ac_user 用户表
CREATE TABLE IF NOT EXISTS ac_user (
    id                  varchar(32)  NOT NULL DEFAULT replace(gen_random_uuid()::text,'-',''),
    tenant_id           varchar(32)  NOT NULL,
    name                varchar(100) NOT NULL,
    email               varchar(255) NOT NULL,
    password_hash       varchar(255) NOT NULL,
    role                varchar(30)  NOT NULL DEFAULT 'operator',
    status              varchar(20)  NOT NULL DEFAULT 'active',
    last_login_time     timestamptz,
    login_fail_count    integer      DEFAULT 0,
    locked_until        timestamptz,
    create_user_id      varchar(32),
    create_user_name    varchar(100),
    update_user_id      varchar(32),
    update_user_name    varchar(100),
    create_time         timestamptz  NOT NULL DEFAULT now(),
    update_time         timestamptz  NOT NULL DEFAULT now(),
    is_deleted          smallint     NOT NULL DEFAULT 0,
    CONSTRAINT pk_ac_user PRIMARY KEY (id),
    CONSTRAINT chk_ac_user_role CHECK (role IN ('tenant_admin','operator','designer','auditor')),
    CONSTRAINT chk_ac_user_status CHECK (status IN ('active','disabled','locked'))
);
CREATE UNIQUE INDEX IF NOT EXISTS uq_ac_user_tenant_email ON ac_user (tenant_id, email) WHERE is_deleted = 0;
CREATE INDEX IF NOT EXISTS idx_ac_user_tenant_role ON ac_user (tenant_id, role);
CREATE INDEX IF NOT EXISTS idx_ac_user_status ON ac_user (status);
COMMENT ON TABLE  ac_user IS '用户表：租户内账号，含角色权限';
COMMENT ON COLUMN ac_user.id IS '用户ID（32位UUID）';
COMMENT ON COLUMN ac_user.tenant_id IS '租户ID';
COMMENT ON COLUMN ac_user.name IS '用户姓名';
COMMENT ON COLUMN ac_user.email IS '登录邮箱（租户内唯一）';
COMMENT ON COLUMN ac_user.password_hash IS 'bcrypt密码哈希';
COMMENT ON COLUMN ac_user.role IS '角色：tenant_admin/operator/designer/auditor';
COMMENT ON COLUMN ac_user.status IS '状态：active/disabled/locked';
COMMENT ON COLUMN ac_user.last_login_time IS '最后登录时间';
COMMENT ON COLUMN ac_user.login_fail_count IS '连续登录失败次数';
COMMENT ON COLUMN ac_user.locked_until IS '账号锁定截止时间';
COMMENT ON COLUMN ac_user.create_user_id IS '创建人ID';
COMMENT ON COLUMN ac_user.create_user_name IS '创建人姓名';
COMMENT ON COLUMN ac_user.update_user_id IS '更新人ID';
COMMENT ON COLUMN ac_user.update_user_name IS '更新人姓名';
COMMENT ON COLUMN ac_user.create_time IS '创建时间';
COMMENT ON COLUMN ac_user.update_time IS '更新时间';
COMMENT ON COLUMN ac_user.is_deleted IS '逻辑删除：0=正常 1=已删除';

-- 1.3 ac_provider 模型厂商表
CREATE TABLE IF NOT EXISTS ac_provider (
    id                  varchar(32)  NOT NULL DEFAULT replace(gen_random_uuid()::text,'-',''),
    tenant_id           varchar(32)  NOT NULL,
    type                varchar(20)  NOT NULL,
    name                varchar(100) NOT NULL,
    base_url            varchar(500),
    api_key_encrypted   text,
    api_key_masked      varchar(50),
    model_name          varchar(100),
    is_default          smallint     DEFAULT 0,
    status              varchar(20)  NOT NULL DEFAULT 'active',
    extra_config        jsonb        DEFAULT '{}',
    create_user_id      varchar(32),
    create_user_name    varchar(100),
    update_user_id      varchar(32),
    update_user_name    varchar(100),
    create_time         timestamptz  NOT NULL DEFAULT now(),
    update_time         timestamptz  NOT NULL DEFAULT now(),
    is_deleted          smallint     NOT NULL DEFAULT 0,
    CONSTRAINT pk_ac_provider PRIMARY KEY (id),
    CONSTRAINT chk_ac_provider_type CHECK (type IN ('llm','image','video')),
    CONSTRAINT chk_ac_provider_status CHECK (status IN ('active','inactive'))
);
CREATE INDEX IF NOT EXISTS idx_ac_provider_tenant_type ON ac_provider (tenant_id, type);
CREATE INDEX IF NOT EXISTS idx_ac_provider_tenant_default ON ac_provider (tenant_id, type, is_default) WHERE is_deleted = 0;
COMMENT ON TABLE  ac_provider IS '模型厂商表：LLM/图片/视频厂商配置与密钥';
COMMENT ON COLUMN ac_provider.id IS '厂商配置ID（32位UUID）';
COMMENT ON COLUMN ac_provider.tenant_id IS '租户ID';
COMMENT ON COLUMN ac_provider.type IS '厂商类型：llm/image/video';
COMMENT ON COLUMN ac_provider.name IS '厂商名称';
COMMENT ON COLUMN ac_provider.base_url IS 'API服务地址';
COMMENT ON COLUMN ac_provider.api_key_encrypted IS '加密后的API Key';
COMMENT ON COLUMN ac_provider.api_key_masked IS '脱敏展示的API Key';
COMMENT ON COLUMN ac_provider.model_name IS '模型名称';
COMMENT ON COLUMN ac_provider.is_default IS '是否默认厂商：0=否 1=是';
COMMENT ON COLUMN ac_provider.status IS '状态：active/inactive';
COMMENT ON COLUMN ac_provider.extra_config IS '额外配置(JSONB)';
COMMENT ON COLUMN ac_provider.create_user_id IS '创建人ID';
COMMENT ON COLUMN ac_provider.create_user_name IS '创建人姓名';
COMMENT ON COLUMN ac_provider.update_user_id IS '更新人ID';
COMMENT ON COLUMN ac_provider.update_user_name IS '更新人姓名';
COMMENT ON COLUMN ac_provider.create_time IS '创建时间';
COMMENT ON COLUMN ac_provider.update_time IS '更新时间';
COMMENT ON COLUMN ac_provider.is_deleted IS '逻辑删除：0=正常 1=已删除';

-- 1.4 ac_knowledge_base_doc 知识库文档表
CREATE TABLE IF NOT EXISTS ac_knowledge_base_doc (
    id                  varchar(32)  NOT NULL DEFAULT replace(gen_random_uuid()::text,'-',''),
    tenant_id           varchar(32)  NOT NULL,
    category            varchar(50)  NOT NULL,
    doc_type            varchar(50)  NOT NULL,
    name                varchar(500) NOT NULL,
    file_type           varchar(20),
    file_url            varchar(1000),
    file_size           bigint,
    vector_status       varchar(20)  NOT NULL DEFAULT 'pending',
    chunk_count         integer      DEFAULT 0,
    error_message       text,
    metadata            jsonb        DEFAULT '{}',
    create_user_id      varchar(32),
    create_user_name    varchar(100),
    update_user_id      varchar(32),
    update_user_name    varchar(100),
    create_time         timestamptz  NOT NULL DEFAULT now(),
    update_time         timestamptz  NOT NULL DEFAULT now(),
    is_deleted          smallint     NOT NULL DEFAULT 0,
    CONSTRAINT pk_ac_knowledge_base_doc PRIMARY KEY (id),
    CONSTRAINT chk_ac_kb_doc_category CHECK (category IN ('brand_visual','listing','compliance','category')),
    CONSTRAINT chk_ac_kb_doc_type CHECK (doc_type IN ('brand_guide','listing_guide','compliance_rule','category_knowledge','case_study')),
    CONSTRAINT chk_ac_kb_doc_vector_status CHECK (vector_status IN ('pending','processing','success','failed'))
);
CREATE INDEX IF NOT EXISTS idx_ac_kb_doc_tenant_category ON ac_knowledge_base_doc (tenant_id, category);
CREATE INDEX IF NOT EXISTS idx_ac_kb_doc_tenant_status ON ac_knowledge_base_doc (tenant_id, vector_status);
CREATE INDEX IF NOT EXISTS idx_ac_kb_doc_tenant_type ON ac_knowledge_base_doc (tenant_id, doc_type);
COMMENT ON TABLE  ac_knowledge_base_doc IS '知识库文档表：RAG文档，四类分类，向量化状态';
COMMENT ON COLUMN ac_knowledge_base_doc.id IS '文档ID（32位UUID）';
COMMENT ON COLUMN ac_knowledge_base_doc.tenant_id IS '租户ID';
COMMENT ON COLUMN ac_knowledge_base_doc.category IS '分类：brand_visual/listing/compliance/category';
COMMENT ON COLUMN ac_knowledge_base_doc.doc_type IS '文档类型：brand_guide/listing_guide/compliance_rule/category_knowledge/case_study';
COMMENT ON COLUMN ac_knowledge_base_doc.name IS '文档名称';
COMMENT ON COLUMN ac_knowledge_base_doc.file_type IS '文件类型：pdf/txt/md/docx';
COMMENT ON COLUMN ac_knowledge_base_doc.file_url IS '文件存储路径';
COMMENT ON COLUMN ac_knowledge_base_doc.file_size IS '文件大小(字节)';
COMMENT ON COLUMN ac_knowledge_base_doc.vector_status IS '向量化状态：pending/processing/success/failed';
COMMENT ON COLUMN ac_knowledge_base_doc.chunk_count IS '分块数量';
COMMENT ON COLUMN ac_knowledge_base_doc.error_message IS '失败原因';
COMMENT ON COLUMN ac_knowledge_base_doc.metadata IS '扩展元数据(JSONB)';
COMMENT ON COLUMN ac_knowledge_base_doc.create_user_id IS '创建人ID';
COMMENT ON COLUMN ac_knowledge_base_doc.create_user_name IS '创建人姓名';
COMMENT ON COLUMN ac_knowledge_base_doc.update_user_id IS '更新人ID';
COMMENT ON COLUMN ac_knowledge_base_doc.update_user_name IS '更新人姓名';
COMMENT ON COLUMN ac_knowledge_base_doc.create_time IS '创建时间';
COMMENT ON COLUMN ac_knowledge_base_doc.update_time IS '更新时间';
COMMENT ON COLUMN ac_knowledge_base_doc.is_deleted IS '逻辑删除：0=正常 1=已删除';

-- 1.5 ac_knowledge_chunk 知识库分块表（向量）
CREATE TABLE IF NOT EXISTS ac_knowledge_chunk (
    id                  varchar(32)  NOT NULL DEFAULT replace(gen_random_uuid()::text,'-',''),
    tenant_id           varchar(32)  NOT NULL,
    doc_id              varchar(32)  NOT NULL,
    chunk_index         integer      NOT NULL,
    content             text         NOT NULL,
    embedding           vector(1024),
    doc_type            varchar(50)  NOT NULL,
    metadata            jsonb        DEFAULT '{}',
    create_time         timestamptz  NOT NULL DEFAULT now(),
    update_time         timestamptz  NOT NULL DEFAULT now(),
    CONSTRAINT pk_ac_knowledge_chunk PRIMARY KEY (id)
);
CREATE INDEX IF NOT EXISTS idx_ac_kb_chunk_tenant_doc ON ac_knowledge_chunk (tenant_id, doc_id);
CREATE INDEX IF NOT EXISTS idx_ac_kb_chunk_tenant_type ON ac_knowledge_chunk (tenant_id, doc_type);
CREATE INDEX IF NOT EXISTS idx_ac_kb_chunk_metadata ON ac_knowledge_chunk USING GIN (metadata);
CREATE INDEX IF NOT EXISTS idx_ac_kb_chunk_embedding_hnsw
    ON ac_knowledge_chunk USING hnsw (embedding vector_cosine_ops)
    WITH (m = 16, ef_construction = 64);
COMMENT ON TABLE  ac_knowledge_chunk IS '知识库分块表：文档切分与向量，支持pgvector相似度检索';
COMMENT ON COLUMN ac_knowledge_chunk.id IS '分块ID（32位UUID）';
COMMENT ON COLUMN ac_knowledge_chunk.tenant_id IS '租户ID';
COMMENT ON COLUMN ac_knowledge_chunk.doc_id IS '文档ID';
COMMENT ON COLUMN ac_knowledge_chunk.chunk_index IS '分块序号';
COMMENT ON COLUMN ac_knowledge_chunk.content IS '分块文本内容';
COMMENT ON COLUMN ac_knowledge_chunk.embedding IS '向量(1024维)';
COMMENT ON COLUMN ac_knowledge_chunk.doc_type IS '文档类型（冗余字段）';
COMMENT ON COLUMN ac_knowledge_chunk.metadata IS '元数据(JSONB)';
COMMENT ON COLUMN ac_knowledge_chunk.create_time IS '创建时间';
COMMENT ON COLUMN ac_knowledge_chunk.update_time IS '更新时间';

-- 1.6 ac_sku 商品SKU表
CREATE TABLE IF NOT EXISTS ac_sku (
    id                  varchar(32)  NOT NULL DEFAULT replace(gen_random_uuid()::text,'-',''),
    tenant_id           varchar(32)  NOT NULL,
    sku_code            varchar(100) NOT NULL,
    name                varchar(500) NOT NULL,
    platform            varchar(50),
    category            varchar(200),
    description         text,
    status              varchar(20)  NOT NULL DEFAULT 'active',
    metadata            jsonb        DEFAULT '{}',
    create_user_id      varchar(32),
    create_user_name    varchar(100),
    update_user_id      varchar(32),
    update_user_name    varchar(100),
    create_time         timestamptz  NOT NULL DEFAULT now(),
    update_time         timestamptz  NOT NULL DEFAULT now(),
    is_deleted          smallint     NOT NULL DEFAULT 0,
    CONSTRAINT pk_ac_sku PRIMARY KEY (id),
    CONSTRAINT chk_ac_sku_status CHECK (status IN ('active','archived'))
);
CREATE UNIQUE INDEX IF NOT EXISTS uq_ac_sku_tenant_code ON ac_sku (tenant_id, sku_code) WHERE is_deleted = 0;
CREATE INDEX IF NOT EXISTS idx_ac_sku_tenant_platform ON ac_sku (tenant_id, platform);
CREATE INDEX IF NOT EXISTS idx_ac_sku_tenant_status ON ac_sku (tenant_id, status);
COMMENT ON TABLE  ac_sku IS '商品SKU表：商品唯一标识，关联生成任务与刊登任务';
COMMENT ON COLUMN ac_sku.id IS 'SKU内部ID（32位UUID）';
COMMENT ON COLUMN ac_sku.tenant_id IS '租户ID';
COMMENT ON COLUMN ac_sku.sku_code IS 'SKU编码（租户内唯一）';
COMMENT ON COLUMN ac_sku.name IS '商品名称';
COMMENT ON COLUMN ac_sku.platform IS '目标平台：amazon/tiktok_shop/shopify/temu';
COMMENT ON COLUMN ac_sku.category IS '商品类目';
COMMENT ON COLUMN ac_sku.description IS '商品描述';
COMMENT ON COLUMN ac_sku.status IS '状态：active/archived';
COMMENT ON COLUMN ac_sku.metadata IS '扩展属性(JSONB)';
COMMENT ON COLUMN ac_sku.create_user_id IS '创建人ID';
COMMENT ON COLUMN ac_sku.create_user_name IS '创建人姓名';
COMMENT ON COLUMN ac_sku.update_user_id IS '更新人ID';
COMMENT ON COLUMN ac_sku.update_user_name IS '更新人姓名';
COMMENT ON COLUMN ac_sku.create_time IS '创建时间';
COMMENT ON COLUMN ac_sku.update_time IS '更新时间';
COMMENT ON COLUMN ac_sku.is_deleted IS '逻辑删除：0=正常 1=已删除';

-- 1.7 ac_batch 批次表
CREATE TABLE IF NOT EXISTS ac_batch (
    id                  varchar(32)  NOT NULL DEFAULT replace(gen_random_uuid()::text,'-',''),
    tenant_id           varchar(32)  NOT NULL,
    name                varchar(200),
    sku_count           integer      NOT NULL DEFAULT 0,
    config              jsonb        DEFAULT '{}',
    status              varchar(20)  NOT NULL DEFAULT 'pending',
    create_user_id      varchar(32),
    create_user_name    varchar(100),
    update_user_id      varchar(32),
    update_user_name    varchar(100),
    create_time         timestamptz  NOT NULL DEFAULT now(),
    update_time         timestamptz  NOT NULL DEFAULT now(),
    is_deleted          smallint     NOT NULL DEFAULT 0,
    CONSTRAINT pk_ac_batch PRIMARY KEY (id),
    CONSTRAINT chk_ac_batch_status CHECK (status IN ('pending','running','completed','partial_failed','failed'))
);
CREATE INDEX IF NOT EXISTS idx_ac_batch_tenant_status ON ac_batch (tenant_id, status);
CREATE INDEX IF NOT EXISTS idx_ac_batch_tenant_time ON ac_batch (tenant_id, create_time DESC);
COMMENT ON TABLE  ac_batch IS '批次表：一次批量提交的N个SKU';
COMMENT ON COLUMN ac_batch.id IS '批次ID（32位UUID）';
COMMENT ON COLUMN ac_batch.tenant_id IS '租户ID';
COMMENT ON COLUMN ac_batch.name IS '批次名称';
COMMENT ON COLUMN ac_batch.sku_count IS 'SKU数量';
COMMENT ON COLUMN ac_batch.config IS '批次配置(JSONB)';
COMMENT ON COLUMN ac_batch.status IS '状态：pending/running/completed/partial_failed/failed';
COMMENT ON COLUMN ac_batch.create_user_id IS '创建人ID';
COMMENT ON COLUMN ac_batch.create_user_name IS '创建人姓名';
COMMENT ON COLUMN ac_batch.update_user_id IS '更新人ID';
COMMENT ON COLUMN ac_batch.update_user_name IS '更新人姓名';
COMMENT ON COLUMN ac_batch.create_time IS '创建时间';
COMMENT ON COLUMN ac_batch.update_time IS '更新时间';
COMMENT ON COLUMN ac_batch.is_deleted IS '逻辑删除：0=正常 1=已删除';

-- 1.8 ac_task 生成任务表（核心表）
CREATE TABLE IF NOT EXISTS ac_task (
    id                  varchar(32)  NOT NULL DEFAULT replace(gen_random_uuid()::text,'-',''),
    tenant_id           varchar(32)  NOT NULL,
    batch_id            varchar(32)  NOT NULL,
    sku_id              varchar(32)  NOT NULL,
    config              jsonb        NOT NULL DEFAULT '{}',
    status              varchar(20)  NOT NULL DEFAULT 'pending',
    progress            integer      DEFAULT 0,
    current_node        varchar(50),
    retry_count         integer      DEFAULT 0,
    max_retries         integer      DEFAULT 2,
    error               text,
    trace_id            varchar(64),
    started_at          timestamptz,
    completed_at        timestamptz,
    total_token_usage   jsonb        DEFAULT '{}',
    total_cost          numeric(12,6) DEFAULT 0,
    create_user_id      varchar(32),
    create_user_name    varchar(100),
    update_user_id      varchar(32),
    update_user_name    varchar(100),
    create_time         timestamptz  NOT NULL DEFAULT now(),
    update_time         timestamptz  NOT NULL DEFAULT now(),
    is_deleted          smallint     NOT NULL DEFAULT 0,
    CONSTRAINT pk_ac_task PRIMARY KEY (id),
    CONSTRAINT chk_ac_task_status CHECK (status IN ('pending','running','completed','failed','cancelled')),
    CONSTRAINT chk_ac_task_progress CHECK (progress >= 0 AND progress <= 100)
);
CREATE INDEX IF NOT EXISTS idx_ac_task_tenant_batch ON ac_task (tenant_id, batch_id);
CREATE INDEX IF NOT EXISTS idx_ac_task_tenant_sku ON ac_task (tenant_id, sku_id);
CREATE INDEX IF NOT EXISTS idx_ac_task_tenant_status ON ac_task (tenant_id, status);
CREATE INDEX IF NOT EXISTS idx_ac_task_tenant_time ON ac_task (tenant_id, create_time DESC);
CREATE INDEX IF NOT EXISTS idx_ac_task_trace_id ON ac_task (trace_id) WHERE trace_id IS NOT NULL;
COMMENT ON TABLE  ac_task IS '生成任务表（核心表）：单个SKU的生成编排实例';
COMMENT ON COLUMN ac_task.id IS '任务ID（32位UUID）';
COMMENT ON COLUMN ac_task.tenant_id IS '租户ID';
COMMENT ON COLUMN ac_task.batch_id IS '批次ID';
COMMENT ON COLUMN ac_task.sku_id IS 'SKU ID';
COMMENT ON COLUMN ac_task.config IS '任务配置(JSONB)';
COMMENT ON COLUMN ac_task.status IS '状态：pending/running/completed/failed/cancelled';
COMMENT ON COLUMN ac_task.progress IS '进度 0-100';
COMMENT ON COLUMN ac_task.current_node IS '当前执行节点';
COMMENT ON COLUMN ac_task.retry_count IS '已重试次数';
COMMENT ON COLUMN ac_task.max_retries IS '最大重试次数，默认2';
COMMENT ON COLUMN ac_task.error IS '错误信息';
COMMENT ON COLUMN ac_task.trace_id IS '追踪ID';
COMMENT ON COLUMN ac_task.started_at IS '开始执行时间';
COMMENT ON COLUMN ac_task.completed_at IS '完成时间';
COMMENT ON COLUMN ac_task.total_token_usage IS '总Token消耗(JSONB)';
COMMENT ON COLUMN ac_task.total_cost IS '总费用';
COMMENT ON COLUMN ac_task.create_user_id IS '创建人ID';
COMMENT ON COLUMN ac_task.create_user_name IS '创建人姓名';
COMMENT ON COLUMN ac_task.update_user_id IS '更新人ID';
COMMENT ON COLUMN ac_task.update_user_name IS '更新人姓名';
COMMENT ON COLUMN ac_task.create_time IS '创建时间';
COMMENT ON COLUMN ac_task.update_time IS '更新时间';
COMMENT ON COLUMN ac_task.is_deleted IS '逻辑删除：0=正常 1=已删除';

-- 1.9 ac_task_node_run 节点执行记录表（核心表）
CREATE TABLE IF NOT EXISTS ac_task_node_run (
    id                  varchar(32)  NOT NULL DEFAULT replace(gen_random_uuid()::text,'-',''),
    tenant_id           varchar(32)  NOT NULL,
    task_id             varchar(32)  NOT NULL,
    node_name           varchar(50)  NOT NULL,
    status              varchar(20)  NOT NULL DEFAULT 'running',
    input               jsonb,
    output              jsonb,
    prompt_system       text,
    prompt_human        text,
    elapsed_ms          integer,
    token_usage         jsonb        DEFAULT '{}',
    cost                numeric(12,6) DEFAULT 0,
    error               text,
    upstream            varchar(50),
    downstream          varchar(50),
    create_time         timestamptz  NOT NULL DEFAULT now(),
    update_time         timestamptz  NOT NULL DEFAULT now(),
    CONSTRAINT pk_ac_task_node_run PRIMARY KEY (id),
    CONSTRAINT chk_ac_tnr_node CHECK (node_name IN (
        'orchestrator','requirement_analyzer','creative_planner',
        'visual_designer','image_generator','video_generator','quality_reviewer'
    )),
    CONSTRAINT chk_ac_tnr_status CHECK (status IN ('running','success','failed'))
);
CREATE INDEX IF NOT EXISTS idx_ac_tnr_tenant_task ON ac_task_node_run (tenant_id, task_id);
CREATE INDEX IF NOT EXISTS idx_ac_tnr_tenant_node ON ac_task_node_run (tenant_id, node_name);
CREATE INDEX IF NOT EXISTS idx_ac_tnr_tenant_status ON ac_task_node_run (tenant_id, status);
CREATE INDEX IF NOT EXISTS idx_ac_tnr_create_time ON ac_task_node_run (create_time DESC);
COMMENT ON TABLE  ac_task_node_run IS '节点执行记录表（核心表）：Agent节点执行过程可观测记录';
COMMENT ON COLUMN ac_task_node_run.id IS '记录ID（32位UUID）';
COMMENT ON COLUMN ac_task_node_run.tenant_id IS '租户ID';
COMMENT ON COLUMN ac_task_node_run.task_id IS '任务ID';
COMMENT ON COLUMN ac_task_node_run.node_name IS '节点名';
COMMENT ON COLUMN ac_task_node_run.status IS '状态：running/success/failed';
COMMENT ON COLUMN ac_task_node_run.input IS '节点输入快照(JSONB)';
COMMENT ON COLUMN ac_task_node_run.output IS '节点输出快照(JSONB)';
COMMENT ON COLUMN ac_task_node_run.prompt_system IS 'System Prompt原文';
COMMENT ON COLUMN ac_task_node_run.prompt_human IS 'Human Prompt原文';
COMMENT ON COLUMN ac_task_node_run.elapsed_ms IS '耗时(毫秒)';
COMMENT ON COLUMN ac_task_node_run.token_usage IS 'Token消耗(JSONB)';
COMMENT ON COLUMN ac_task_node_run.cost IS '费用';
COMMENT ON COLUMN ac_task_node_run.error IS '错误信息';
COMMENT ON COLUMN ac_task_node_run.upstream IS '上游节点名';
COMMENT ON COLUMN ac_task_node_run.downstream IS '下游节点名';
COMMENT ON COLUMN ac_task_node_run.create_time IS '创建时间';
COMMENT ON COLUMN ac_task_node_run.update_time IS '更新时间';

-- 1.10 ac_copy 文案内容表
CREATE TABLE IF NOT EXISTS ac_copy (
    id                  varchar(32)  NOT NULL DEFAULT replace(gen_random_uuid()::text,'-',''),
    tenant_id           varchar(32)  NOT NULL,
    task_id             varchar(32)  NOT NULL,
    sku_id              varchar(32)  NOT NULL,
    language            varchar(10)  NOT NULL DEFAULT 'zh',
    title               text,
    bullets             jsonb        DEFAULT '[]',
    description         text,
    keywords            jsonb        DEFAULT '[]',
    status              varchar(20)  NOT NULL DEFAULT 'draft',
    create_user_id      varchar(32),
    create_user_name    varchar(100),
    update_user_id      varchar(32),
    update_user_name    varchar(100),
    create_time         timestamptz  NOT NULL DEFAULT now(),
    update_time         timestamptz  NOT NULL DEFAULT now(),
    is_deleted          smallint     NOT NULL DEFAULT 0,
    CONSTRAINT pk_ac_copy PRIMARY KEY (id),
    CONSTRAINT chk_ac_copy_status CHECK (status IN ('draft','reviewed','approved'))
);
CREATE INDEX IF NOT EXISTS idx_ac_copy_tenant_task ON ac_copy (tenant_id, task_id);
CREATE INDEX IF NOT EXISTS idx_ac_copy_tenant_sku ON ac_copy (tenant_id, sku_id);
CREATE INDEX IF NOT EXISTS idx_ac_copy_tenant_status ON ac_copy (tenant_id, status);
COMMENT ON TABLE  ac_copy IS '文案内容表：创意生成智能体产出的结构化文案';
COMMENT ON COLUMN ac_copy.id IS '文案ID（32位UUID）';
COMMENT ON COLUMN ac_copy.tenant_id IS '租户ID';
COMMENT ON COLUMN ac_copy.task_id IS '任务ID';
COMMENT ON COLUMN ac_copy.sku_id IS 'SKU ID';
COMMENT ON COLUMN ac_copy.language IS '语言，默认zh';
COMMENT ON COLUMN ac_copy.title IS '商品标题';
COMMENT ON COLUMN ac_copy.bullets IS '五点描述(JSONB数组)';
COMMENT ON COLUMN ac_copy.description IS '长描述';
COMMENT ON COLUMN ac_copy.keywords IS '搜索关键词(JSONB数组)';
COMMENT ON COLUMN ac_copy.status IS '状态：draft/reviewed/approved';
COMMENT ON COLUMN ac_copy.create_user_id IS '创建人ID';
COMMENT ON COLUMN ac_copy.create_user_name IS '创建人姓名';
COMMENT ON COLUMN ac_copy.update_user_id IS '更新人ID';
COMMENT ON COLUMN ac_copy.update_user_name IS '更新人姓名';
COMMENT ON COLUMN ac_copy.create_time IS '创建时间';
COMMENT ON COLUMN ac_copy.update_time IS '更新时间';
COMMENT ON COLUMN ac_copy.is_deleted IS '逻辑删除：0=正常 1=已删除';

-- 1.11 ac_asset 素材资产表
CREATE TABLE IF NOT EXISTS ac_asset (
    id                  varchar(32)  NOT NULL DEFAULT replace(gen_random_uuid()::text,'-',''),
    tenant_id           varchar(32)  NOT NULL,
    task_id             varchar(32)  NOT NULL,
    sku_id              varchar(32)  NOT NULL,
    type                varchar(20)  NOT NULL,
    sub_type            varchar(30),
    url                 varchar(1000),
    meta                jsonb        DEFAULT '{}',
    status              varchar(20)  NOT NULL DEFAULT 'pending',
    is_mock             smallint     DEFAULT 0,
    create_user_id      varchar(32),
    create_user_name    varchar(100),
    update_user_id      varchar(32),
    update_user_name    varchar(100),
    create_time         timestamptz  NOT NULL DEFAULT now(),
    update_time         timestamptz  NOT NULL DEFAULT now(),
    is_deleted          smallint     NOT NULL DEFAULT 0,
    CONSTRAINT pk_ac_asset PRIMARY KEY (id),
    CONSTRAINT chk_ac_asset_type CHECK (type IN ('image','video','storyboard','copy_file')),
    CONSTRAINT chk_ac_asset_status CHECK (status IN ('pending','approved','rejected','failed'))
);
CREATE INDEX IF NOT EXISTS idx_ac_asset_tenant_task ON ac_asset (tenant_id, task_id);
CREATE INDEX IF NOT EXISTS idx_ac_asset_tenant_sku ON ac_asset (tenant_id, sku_id);
CREATE INDEX IF NOT EXISTS idx_ac_asset_tenant_type_status ON ac_asset (tenant_id, type, status);
COMMENT ON TABLE  ac_asset IS '素材资产表：图片/视频/分镜脚本等产物';
COMMENT ON COLUMN ac_asset.id IS '资产ID（32位UUID）';
COMMENT ON COLUMN ac_asset.tenant_id IS '租户ID';
COMMENT ON COLUMN ac_asset.task_id IS '任务ID';
COMMENT ON COLUMN ac_asset.sku_id IS 'SKU ID';
COMMENT ON COLUMN ac_asset.type IS '类型：image/video/storyboard/copy_file';
COMMENT ON COLUMN ac_asset.sub_type IS '子类型：main/scene/selling_point';
COMMENT ON COLUMN ac_asset.url IS '存储路径';
COMMENT ON COLUMN ac_asset.meta IS '元数据(JSONB)';
COMMENT ON COLUMN ac_asset.status IS '状态：pending/approved/rejected/failed';
COMMENT ON COLUMN ac_asset.is_mock IS '是否为Mock资产：0=否 1=是';
COMMENT ON COLUMN ac_asset.create_user_id IS '创建人ID';
COMMENT ON COLUMN ac_asset.create_user_name IS '创建人姓名';
COMMENT ON COLUMN ac_asset.update_user_id IS '更新人ID';
COMMENT ON COLUMN ac_asset.update_user_name IS '更新人姓名';
COMMENT ON COLUMN ac_asset.create_time IS '创建时间';
COMMENT ON COLUMN ac_asset.update_time IS '更新时间';
COMMENT ON COLUMN ac_asset.is_deleted IS '逻辑删除：0=正常 1=已删除';

-- 1.12 ac_compliance_report 合规报告表
CREATE TABLE IF NOT EXISTS ac_compliance_report (
    id                  varchar(32)  NOT NULL DEFAULT replace(gen_random_uuid()::text,'-',''),
    tenant_id           varchar(32)  NOT NULL,
    task_id             varchar(32)  NOT NULL,
    sku_id              varchar(32)  NOT NULL,
    score               numeric(5,2),
    dimensions          jsonb        DEFAULT '{}',
    violations          jsonb        DEFAULT '[]',
    passed              smallint     DEFAULT 0,
    suggestions         jsonb        DEFAULT '[]',
    status              varchar(20)  NOT NULL DEFAULT 'pending',
    create_user_id      varchar(32),
    create_user_name    varchar(100),
    update_user_id      varchar(32),
    update_user_name    varchar(100),
    create_time         timestamptz  NOT NULL DEFAULT now(),
    update_time         timestamptz  NOT NULL DEFAULT now(),
    is_deleted          smallint     NOT NULL DEFAULT 0,
    CONSTRAINT pk_ac_compliance_report PRIMARY KEY (id)
);
CREATE INDEX IF NOT EXISTS idx_ac_cr_tenant_task ON ac_compliance_report (tenant_id, task_id);
CREATE INDEX IF NOT EXISTS idx_ac_cr_tenant_sku ON ac_compliance_report (tenant_id, sku_id);
CREATE INDEX IF NOT EXISTS idx_ac_cr_tenant_passed ON ac_compliance_report (tenant_id, passed);
COMMENT ON TABLE  ac_compliance_report IS '合规报告表：质量评估智能体产出的合规/质量结果';
COMMENT ON COLUMN ac_compliance_report.id IS '报告ID（32位UUID）';
COMMENT ON COLUMN ac_compliance_report.tenant_id IS '租户ID';
COMMENT ON COLUMN ac_compliance_report.task_id IS '任务ID';
COMMENT ON COLUMN ac_compliance_report.sku_id IS 'SKU ID';
COMMENT ON COLUMN ac_compliance_report.score IS '总分';
COMMENT ON COLUMN ac_compliance_report.dimensions IS '各维度得分(JSONB)';
COMMENT ON COLUMN ac_compliance_report.violations IS '违规项列表(JSONB数组)';
COMMENT ON COLUMN ac_compliance_report.passed IS '是否通过：0=否 1=是';
COMMENT ON COLUMN ac_compliance_report.suggestions IS '建议列表(JSONB数组)';
COMMENT ON COLUMN ac_compliance_report.status IS '状态';
COMMENT ON COLUMN ac_compliance_report.create_user_id IS '创建人ID';
COMMENT ON COLUMN ac_compliance_report.create_user_name IS '创建人姓名';
COMMENT ON COLUMN ac_compliance_report.update_user_id IS '更新人ID';
COMMENT ON COLUMN ac_compliance_report.update_user_name IS '更新人姓名';
COMMENT ON COLUMN ac_compliance_report.create_time IS '创建时间';
COMMENT ON COLUMN ac_compliance_report.update_time IS '更新时间';
COMMENT ON COLUMN ac_compliance_report.is_deleted IS '逻辑删除：0=正常 1=已删除';

-- 1.13 ac_operation_log 操作日志表
CREATE TABLE IF NOT EXISTS ac_operation_log (
    id                  varchar(32)  NOT NULL DEFAULT replace(gen_random_uuid()::text,'-',''),
    tenant_id           varchar(32)  NOT NULL,
    user_id             varchar(32),
    user_name           varchar(100),
    action              varchar(50)  NOT NULL,
    target_type         varchar(50),
    target_id           varchar(32),
    detail              jsonb        DEFAULT '{}',
    ip_address          varchar(45),
    user_agent          varchar(500),
    create_time         timestamptz  NOT NULL DEFAULT now(),
    CONSTRAINT pk_ac_operation_log PRIMARY KEY (id)
);
CREATE INDEX IF NOT EXISTS idx_ac_olog_tenant_user ON ac_operation_log (tenant_id, user_id);
CREATE INDEX IF NOT EXISTS idx_ac_olog_tenant_action ON ac_operation_log (tenant_id, action);
CREATE INDEX IF NOT EXISTS idx_ac_olog_create_time ON ac_operation_log (create_time DESC);
COMMENT ON TABLE  ac_operation_log IS '操作日志表：用户操作与下载日志，保留≥30天';
COMMENT ON COLUMN ac_operation_log.id IS '日志ID（32位UUID）';
COMMENT ON COLUMN ac_operation_log.tenant_id IS '租户ID';
COMMENT ON COLUMN ac_operation_log.user_id IS '操作人ID';
COMMENT ON COLUMN ac_operation_log.user_name IS '操作人姓名';
COMMENT ON COLUMN ac_operation_log.action IS '动作：login/create_task/download/...';
COMMENT ON COLUMN ac_operation_log.target_type IS '目标类型';
COMMENT ON COLUMN ac_operation_log.target_id IS '目标ID';
COMMENT ON COLUMN ac_operation_log.detail IS '详情(JSONB)';
COMMENT ON COLUMN ac_operation_log.ip_address IS '客户端IP';
COMMENT ON COLUMN ac_operation_log.user_agent IS 'User-Agent';
COMMENT ON COLUMN ac_operation_log.create_time IS '创建时间';

-- 1.14 ac_listing 刊登记录表（二期）
CREATE TABLE IF NOT EXISTS ac_listing (
    id                  varchar(32)  NOT NULL DEFAULT replace(gen_random_uuid()::text,'-',''),
    tenant_id           varchar(32)  NOT NULL,
    sku_id              varchar(32)  NOT NULL,
    platform            varchar(50)  NOT NULL,
    external_id         varchar(200),
    status              varchar(20)  NOT NULL DEFAULT 'pending',
    error               text,
    request_payload     jsonb,
    response_payload    jsonb,
    create_user_id      varchar(32),
    create_user_name    varchar(100),
    update_user_id      varchar(32),
    update_user_name    varchar(100),
    create_time         timestamptz  NOT NULL DEFAULT now(),
    update_time         timestamptz  NOT NULL DEFAULT now(),
    is_deleted          smallint     NOT NULL DEFAULT 0,
    CONSTRAINT pk_ac_listing PRIMARY KEY (id),
    CONSTRAINT chk_ac_listing_platform CHECK (platform IN ('amazon','tiktok_shop','shopify','temu')),
    CONSTRAINT chk_ac_listing_status CHECK (status IN ('pending','success','failed','need_fix'))
);
CREATE INDEX IF NOT EXISTS idx_ac_listing_tenant_sku ON ac_listing (tenant_id, sku_id);
CREATE INDEX IF NOT EXISTS idx_ac_listing_tenant_platform ON ac_listing (tenant_id, platform);
CREATE INDEX IF NOT EXISTS idx_ac_listing_tenant_status ON ac_listing (tenant_id, status);
COMMENT ON TABLE  ac_listing IS '刊登记录表（二期）：推送至外部平台的刊登记录';
COMMENT ON COLUMN ac_listing.id IS '刊登ID（32位UUID）';
COMMENT ON COLUMN ac_listing.tenant_id IS '租户ID';
COMMENT ON COLUMN ac_listing.sku_id IS 'SKU ID';
COMMENT ON COLUMN ac_listing.platform IS '平台：amazon/tiktok_shop/shopify/temu';
COMMENT ON COLUMN ac_listing.external_id IS '平台商品ID';
COMMENT ON COLUMN ac_listing.status IS '状态：pending/success/failed/need_fix';
COMMENT ON COLUMN ac_listing.error IS '错误信息';
COMMENT ON COLUMN ac_listing.request_payload IS '请求载荷(JSONB)';
COMMENT ON COLUMN ac_listing.response_payload IS '响应载荷(JSONB)';
COMMENT ON COLUMN ac_listing.create_user_id IS '创建人ID';
COMMENT ON COLUMN ac_listing.create_user_name IS '创建人姓名';
COMMENT ON COLUMN ac_listing.update_user_id IS '更新人ID';
COMMENT ON COLUMN ac_listing.update_user_name IS '更新人姓名';
COMMENT ON COLUMN ac_listing.create_time IS '创建时间';
COMMENT ON COLUMN ac_listing.update_time IS '更新时间';
COMMENT ON COLUMN ac_listing.is_deleted IS '逻辑删除：0=正常 1=已删除';

-- ============================================================
-- 2. 种子数据
-- ============================================================

-- 2.1 默认租户
INSERT INTO ac_tenant (id, name, status, plan, contact_email, max_skus, max_tokens_daily, create_user_name)
VALUES (
    '00000000000000000000000000000001',
    '默认租户',
    'active',
    'standard',
    'admin@agentic-commerce.local',
    10000,
    1000000,
    'system'
) ON CONFLICT (id) DO NOTHING;

-- 2.2 默认管理员账号（密码: admin123）
INSERT INTO ac_user (id, tenant_id, name, email, password_hash, role, status, create_user_name)
VALUES (
    '00000000000000000000000000000002',
    '00000000000000000000000000000001',
    '系统管理员',
    'admin@agentic-commerce.local',
    '$2b$12$LJ3m4ys3GZfnY0QYk8VJPej8pWqX7ZvY9mN2oP3rT4uV5wX6yZ7a',
    'tenant_admin',
    'active',
    'system'
) ON CONFLICT (id) DO NOTHING;

-- 2.3 默认模型厂商
INSERT INTO ac_provider (id, tenant_id, type, name, base_url, model_name, is_default, status, create_user_name)
VALUES
    (replace(gen_random_uuid()::text,'-',''), '00000000000000000000000000000001', 'llm',   '通义千问', 'https://dashscope.aliyuncs.com/compatible-mode/v1', 'qwen-plus',  1, 'active', 'system'),
    (replace(gen_random_uuid()::text,'-',''), '00000000000000000000000000000001', 'image', '通义万相', 'https://dashscope.aliyuncs.com/api/v1',             'wan2.1',     1, 'active', 'system'),
    (replace(gen_random_uuid()::text,'-',''), '00000000000000000000000000000001', 'video', '可灵Kling', 'https://api.klingai.com',                         'kling-v1',   1, 'active', 'system')
ON CONFLICT DO NOTHING;

-- ============================================================
-- 3. 完成
-- ============================================================
DO $$
BEGIN
    RAISE NOTICE 'AgenticCommerce 数据库初始化完成';
    RAISE NOTICE '表数量: 14';
    RAISE NOTICE '向量维度: 1024';
    RAISE NOTICE '默认租户ID: 00000000000000000000000000000001';
    RAISE NOTICE '默认管理员: admin@agentic-commerce.local / admin123';
END $$;
```

